# IMA 策略 · 最优方式（最终决议）2026-08-24

## 结论（一句话）
把 IMA 知识库因子**作为主动量策略的“降回撤叠加层”**来用，而不是独立主信号；
LLM 情绪（GLM-5.3）当前版本**只做监控/预警，不入实盘**。

## 最终采用配置
- **模式**：`momentum + IMA词频因子`（动量排序 + IMA 词频 z-score 叠加）
- **参数**：`window = 60天`，`IMA 权重 λ = 1.2`，月度调仓，10 只等权，5bp 成本
- **行为**：保留现有动量主仓不动；IMA 叠加版作为影子策略在本仓库继续跟踪

选参依据：样本外（5 段 IS 选参 → 6 段 OOS）——
动量 OOS 夏普 0.37 → 动量+IMA OOS 夏普 0.54，且波动从 17.8% 降到 13.3%。

## 为什么不是别的选择
| 候选 | 全期夏普 | 最大回撤 | 全期期末 | 结论 |
|---|---:|---:|---:|---|
| 基准动量（現主策略） | 2.54 | -39.0% | $47,120 | 保留为实盘主仓 |
| 动量 + IMA 词频 (win60/λ1.2) | ~2.8-3.0 | ~-27~-33% | ~$44-45k | **采用（最优方式）** |
| 纯 IMA 因子 | 2.54 | -17.2% | $35,356 | 收益太低，只降回撤 |
| 动量 + LLM 情绪 (60/强/λ1.0) | 2.80 | -32.8% | $44,494 | 夏普接近，但事件研究/IC 均弱，不采用 |
| LLM 强空过滤 | ≤2.49 | -39% | ~$45-46k | 不优于基准，误杀强势股，不采用 |
| LLM 次日信号策略 | 负超额 | -24% | 亏损 | 不可交易，仅监控 |

## 固化的关键判断（来自实证）
1. IMA 词频因子能**有效降回撤、降波动**，但绝对收益略低于纯动量 → 定位“风控叠加层”。
2. LLM 情绪 IC ≈ 0~负，事件研究 1 日才勉强为正（t≈2），3 日以上为负 → 不能当交易信号。
3. LLM 强空过滤会误杀被看空的强势股 → 不做负面过滤器。
4. 所有 LLM 结论基于当前数据/模型版本，可留作监控提醒（预警文章提及的标的）。

## 当前最优买入名单（截至 2026-07-31 调仓、数据 2026-08-21）
见 `backtest_output/ima_final_top10.csv`。

## 建议下一步
- 实盘主策略（lianghua2）**保持不动**，继续按月频运行。
- 本仓库只跑**影子策略**：用 `backtest_ima_factor.py` 的 `momentum_ima` 模式（window=60, λ=1.2）每周/每月输出 IMA 叠加名单，与实盘对比，而不是立即切换实盘。
- 只有当影子策略连续 3 个月夏普 > 主策略 且回撤明显更低时，才考虑切到实盘。

> 注意：样本仅 11 个月，任何固定参数仍有过拟合风险；未经更长样本外验证前，不建议直接替换实盘。
